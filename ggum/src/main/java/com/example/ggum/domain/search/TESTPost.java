
/*public class TESTPost {
    public static void main(String[] args) {

        // TESTUser에서 생성한 유저 객체를 사용하여 게시글 생성
        User alice = TESTUser.alice;
        User bob = TESTUser.bob;
        User charlie = TESTUser.charlie; // 오타 수정
        User daisy = TESTUser.daisy;

        // 게시글 데이터 생성
        Post post1 = new Post(alice, "Fresh Apples", "Delicious red apples from farm", "apple", 599L);
        Post post2 = new Post(bob, "Organic Bananas", "Fresh and organic bananas", "banana", 349L);
        Post post3 = new Post(charlie, "Latest Smartphone", "New smartphone with 5G", "cell", 79999L);
        Post post4 = new Post(daisy, "Latest Smartphone", "New smartphone with 5G", "cell", 79999L);

        // 게시글 출력 (테스트용)
        System.out.println("Post Title: " + post1.getTitle() + ", Posted by: " + post1.getUser().getUsername());
        System.out.println("Post Title: " + post2.getTitle() + ", Posted by: " + post2.getUser().getUsername());
        System.out.println("Post Title: " + post3.getTitle() + ", Posted by: " + post3.getUser().getUsername());
        System.out.println("Post Title: " + post4.getTitle() + ", Posted by: " + post4.getUser().getUsername());
    }
}
 */
